# URL do banco de dados MongoDB (Atlas ou local)
DATABASE_URL="mongodb+srv://usuario:senha@cluster.mongodb.net/DatabaseName?retryWrites=true&w=majority"

# Chave secreta para JWT - Use uma string aleatória forte!
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="seu-jwt-secret-super-secreto-aqui"

# Ambiente de execução
NODE_ENV="production"

# Porta do servidor
PORT=3000